#!/bin/sh
set -eu

process_text="$HOME/scripts/sc-helpers/process-text-for-speech"
make_audio="$HOME/scripts/sc-helpers/make-spoken-audio-track"

if [ "$#" -gt 0 ]; then
  summary="$("$process_text" "$*")"
else
  summary="$(cat | "$process_text")"
fi

printf "%s" "$summary" | "$make_audio"
