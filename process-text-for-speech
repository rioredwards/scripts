#!/bin/sh
set -eu

text=""
if [ "$#" -gt 0 ]; then
  text="$*"
elif [ ! -t 0 ]; then
  text="$(cat)"
fi

if [ -z "$text" ]; then
  exit 0
fi

printf '%s' "$text" | "$HOME/scripts/aitt" process-text-for-speech
